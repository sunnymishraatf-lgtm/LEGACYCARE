# The database-free demo works without environment variables.
# Recommended on Vercel to replace the built-in demo-only session fallback:
NEXTAUTH_SECRET="generate-a-long-random-value"

# Optional: only needed when enabling the database-backed planning/admin APIs.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"
